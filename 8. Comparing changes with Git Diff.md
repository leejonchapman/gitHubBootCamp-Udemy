# Introducing the Git Diff command

Lists all changes that are not staged from the next commit.

``` diff
diff --git a/index.html b/index.html
index fdb8458..6fe893d 100644
--- a/index.html
+++ b/index.html
@@ -25,7 +25,7 @@
 <body>
   <div class="container">
     <div class="heading">
-      <h1 class="title">2048</h1>
+      <h1 class="title">Hey there!</h1>
       <div class="scores-container">
         <div class="score-container">0</div>
         <div class="best-container">0</div>
@@ -95,6 +95,7 @@
       conceptually similar to <a href="http://asherv.com/threes/" target="_blank">Thre
 es by Asher Vollmer.</a>
     </p>
   </div>
+  <p>CHICKENS!!</p>

   <script src="js/bind_polyfill.js"></script>
   <script src="js/classlist_polyfill.js"></script>
diff --git a/js/grid.js b/js/grid.js
:
```
# A guide to reading Diffs

```diff
diff --git a/rainbow.txt b/rainbow.txt
index 72d1d5a..f2c8117 100644
--- a/rainbow.txt
+++ b/rainbow.txt
@@ -3,4 +3,5 @@ orange
 yellow
 green
 blue
-purple
+indigo
+violet
```

Below shows the two files that are being compared. It will declare one as A and the other as B.

```bash
diff --git a/rainbow.txt b/rainbow.txt
```

This shows the file metadata - it doesnt matter.

```bash
index 72d1d5a..f2c8117 100644
```

Markers are next - changes in file A will be highlighted with a -- sign

```bash
--- a/rainbow.txt
+++ b/rainbow.txt
```

This is called a Chunk header.

From file a (---), 4 lines are extracted starting from line 3.  
From file b (+++), 5 lines are extracted starting from line 3

```bash
@@ -3,4 +3,5 @@ orange
```

A Chunk - before and after a code change. If you changed 1000 lines of code, the diff wouldnt show you everything.

```bash
@@ -3,4 +3,5 @@ orange
 yellow
 green
 blue
-purple
+indigo
+violet
```

This would look like:

|Line|file a|file b|
|---|---|---|
|1|red|red|
|2|orange|orange|
|3|yellow|yellow|
|4|green|green|
|5|blue|blue|
|6|purple|indigo|
|7||violet|

# Viewing unstaged changes

```bash
git diff
```
# Viewing Working Directory Changes

Lists all changes in the working tree since the last commit. This is staged and unstaged changes.

This is files in the working directory. 

```bash
git diff head
```

When you see -- /dev/null it means that the file has just been created.
# Viewing staged changes

```bash
	git diff --staged
```

# Diffing specific files

You can see the changes in a specific file

```bash
git diff contoso.txt
git diff contoso.txt --staged
```
# Comparing changes across branches

```bash
git diff branch1 branch2
```
# Comparing changes across commits

```bash 
git diff commit1 commit2
```
# Labs

https://github.com/Colt/git-diff-exercise

**Part 1: Setup**  
Please run the following commands from your terminal. Make sure you are not inside of a Git repo.  

```bash
git clone https://github.com/Colt/git-diff-exercise
```

  

- Change into the newly created folder called `git-diff-exercise`. You will be on a branch called `current` by default.  
    
- Run `git switch 1970s`  
    
- You should now have two branches that you can move between: `current` and `1970s` Each branch contains two files: `queen.txt` and `fleetwoodmac.txt`

**Part 2: The Diffs**  

1. Compare the difference between the `1970s` branch and the `current` branch across all files  
    
2. Compare the difference between the `1970s` branch and the `current` branch but only for the `queen.txt` file  
    
3. Switch over to the `current` branch if you are not currently on it. Run a diff to compare the current HEAD to the previous commit (the one with the message "replace John Deacon as Queen's bassist"  
    
4. While on the `current` branch, change the `queen.txt` file by replacing Adam Lambert with your own name. Save the change. Add `queen.txt` to the staging area. DO NOT COMMIT YET!  
    
5. Edit the `fleetwoodmac.txt` file, changing the lead singer from Stevie Nicks to Stevie Chicks (one of my chickens). Save the file, but do not stage it.  
    
6. Run a diff that would reveal the unstaged changes in the working directory (you should see only the change to `fleetwoodmac.txt` printed out)  
    
7. Run a diff that would reveal only the staged changes (you should only see the change to `queen.txt` printed out)  
    
8. Run a diff that prints all changes (staged and unstaged) since the prior commit (you should see both changes printed out)
