# Checking out old commits

```git
git checkout 
```

This is the swiss army knife of commands and can be used to do a lot of things such as:
- Switch to new branches
- Restore files
- Undo history

This can be used to view a previous commit and you just need the first seven characters of the commit:

```git
git checkout d8ehdh2
```

To find out your hash for HEAD you can run:

```shell
cat .git/head
```
## Detached HEAD

**A detached HEAD state occurs when Git checks out a specific commit instead of a branch.** Normally HEAD points to a local branch reference. When you check out a commit directly, HEAD points directly to that unique commit id instead.

### What changes

- **Commit tracking** You can still make new commits. However no branch tracks them.
    
- **Moving away** If you switch to another branch your new commits become orphaned. Git will eventually delete them during garbage collection.
    

### Comparisons

|State|What HEAD points to|What happens when you commit|
|---|---|---|
|**Normal**|A branch pointer|The branch pointer moves forward with the new commit|
|**Detached**|A specific commit hash|HEAD moves forward but no branch updates|

### Questions you might have missed

**How do I fix a detached HEAD?** Switch back to your main branch using `git switch main`.

**How do I save work created in this state?** Create a new branch right where you are using `git switch c new branch name`.

**Is a detached HEAD dangerous?** No. You will not lose any existing code. Just remember to create a branch if you want to save new changes.
# Reattaching our detached HEAD
**Here is the content converted to Markdown, complete with Git commands for each option:**

## You have a couple of options:

1. **Stay in detached HEAD** to examine the contents of the old commit. Poke around, view the files, etc.
    
    - _How:_ You don't need to run any commands. Just look at your files or run logs:
        
        ```bash
        git log --oneline
        ```
        
2. **Leave and go back** to wherever you were before—reattach the HEAD.
    
    - _How:_ Switch back to your previous branch (e.g., `main` or `master`):
        
        ```bash
        git checkout main
        # Or using the newer command:
        git switch main
        ```
        
3. **Create a new branch** and switch to it. You can now make and save changes, since HEAD is no longer detached.
    
    - _How:_ Create and switch to a new branch from your current position:
        
        ```bash
        git checkout -b my-new-branch
        # Or using the newer command:
        git switch -c my-new-branch
        ```
# Referencing commits relative to HEAD

HEAD~1 refers to the parent and HEAD~2 refers to the grandparent.
```git
git checkout HEAD~1
```

# Disregarding changes with Git Checkout

Suppose you've made some changes to a file but don't want to keep them. To revert the file back to whatever it looked like when you last committed, you can use:

```git
git checkout HEAD <filename>
``` 
To discard any changes in that file, reverting back to the HEAD.
# Unmodifying with Git Restore

This is a new command and can be used to undo changes since your last commit, at HEAD.
```git
git restore <filename>
```

What about if you wanted to go back two commits?

```git
git restore --source HEAD~2 <filename>
```
# Unstaging files with Git Restore

We want to remove a file that we have staged - we can do this with:

```git
git restore --staged <filename>
```

# Undoing commits with Git Reset

Suppose you committed a change to a master branch, but you want to undo it as you were supposed to be working on a different branch.

```git
git reset <commit-hash>
```

You dont lose the changes you lose the commits.

A hard reset will reset the commits and the files in the working directory too.

```git
git reset --hard <commit-hash>
```
# Reverting commits with Git Revert

Makes a new commit and then undoes the changes - good for when you are collaborating with other people.
```git
git revert <c 
```
# Labs

This exercise is based around the Beatles' song _Yesterday_ and the evolution of its lyrics over time.

1. **Clone the repository** (ensure you are not already in a repo):
    
    `git clone [https://github.com/Colt/yesterday-exercise](https://github.com/Colt/yesterday-exercise)`
    
2. **Navigate** into the newly created directory and view `lyrics.txt`.
    
3. **Inspect** the commit history; you should see 8 commits on the `master` branch.
    
4. **Travel back** by checking out the very first commit (this puts you in detached HEAD). View `lyrics.txt` to verify the change, then return to the `master` branch.
    
5. **Create a branch** named `scrambled-eggs` at the commit with the message "finish original lyrics".
    
6. **Return** to the `master` branch.
    
7. **Simulate an error**: Delete everything in `lyrics.txt` and save the file.
    
8. **Undo the mistake**: Use a Git command to discard the changes made to `lyrics.txt` since the last commit.
    
9. **Add parody lyrics**: Replace the content of `lyrics.txt` with:
    
    Plaintext
    
    ```
    404
    Guess this ain't the page you're looking for
    On this website there are thousands more
    With no error 404
    
    Suddenly
    This is not the page you thought you'd see
    But it's not an error 403
    Yes, 404s come easily
    ```
    
    Add and commit these changes on the `master` branch.
    
10. **Append text**: Add the following to the bottom of `lyrics.txt`:
    
    Plaintext
    
    ```
    Why it has to show, I don't know
    Or what it's for
    You typed something wrong?
    Here's no song - it's 404
    
    404
    Adding 1% to twenty score
    (I put that line in 'cause it scans, no more)
    "Goodbye" from error 404
    ```
    
    Add and commit these changes to the `master` branch.
    
11. **Refactor**: Remove the last two commits from `master` while keeping the changes in your working directory.
    
12. **Create branch**: Create and switch to a new branch called `404`, then add and commit the changes currently in your working directory.
    
13. **Verify**: Switch back to `master` and ensure `lyrics.txt` once again contains the original lyrics to _Yesterday_.
    

### Questions you might have

**What command undoes local file changes?**

You can use `git restore lyrics.txt` or `git checkout -- lyrics.txt`.

**How do I undo two commits while keeping changes?**

Use `git reset --soft HEAD~2`.

**What does the --soft flag do?**

It moves the branch pointer back but keeps your modified files staged and ready to be committed elsewhere.